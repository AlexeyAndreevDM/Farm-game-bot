## 🎮 Система управления аккаунтами

### Что добавлено:

#### 1. Кнопка Account в главном меню
- Появляется во всех inline-клавиатурах рядом с Help
- Позволяет управлять аккаунтами и создавать новые профили

#### 2. Функционал Account:
- **Для новых пользователей:**
  - Приветственное сообщение
  - Предложение создать аккаунт с балансом 50000 ₽
  - Возможность использовать Telegram имя или ввести свой никнейм

- **Для существующих пользователей:**
  - Просмотр информации о текущем аккаунте (никнейм, баланс)
  - Возможность создать новый аккаунт (старый сохраняется в БД)
  - Переключение между профилями

#### 3. Начальный баланс:
- Все новые пользователи получают **50000 ₽**
- Баланс устанавливается автоматически при создании аккаунта
- Применяется как при регистрации через /reg, так и через кнопку Account

### Использование:

#### Создание первого аккаунта:
1. Нажмите кнопку "Account"
2. Выберите "✅ Создать аккаунт"
3. Введите никнейм или используйте Telegram имя
4. Получите 50000 ₽ стартового капитала!

#### Создание дополнительного аккаунта:
1. Нажмите кнопку "Account"
2. Выберите "➕ Создать новый аккаунт"
3. Введите никнейм для нового профиля
4. Старый аккаунт сохранится, новый станет активным

### Технические детали:

#### Связь аккаунта с пользователем:
- Каждый аккаунт привязан к Telegram ID пользователя
- `us_name` = Telegram first_name (для основного аккаунта)
- `us_name` = `{telegram_name}_{nickname}` (для дополнительных аккаунтов)
- State хранится в памяти и переключается при смене аккаунта

#### Файлы:
- [handlers/account.py](handlers/account.py) - обработчики управления аккаунтами
- [database/models.py](database/models.py#L20) - `UserState.money = 50000`
- [config/settings.py](config/settings.py#L14) - `INITIAL_MONEY = 50000`

### Исправления:

#### 1. Проблема с продажей без животных:
**Было:** Бот показывал кнопки выбора животных даже если их нет
**Стало:** 
- Если животных нет → сообщение "🚫 У вас нет животных. Купите сначала через /buy"
- Кнопки выбора НЕ появляются
- Возврат в главное меню
- `desire` очищается автоматически

#### 2. Начальный баланс:
**Проверено:**
- `INITIAL_MONEY = 50000` в config/settings.py ✅
- `UserState.money = 50000` по умолчанию ✅
- При регистрации через /reg → 50000 ₽ ✅
- При создании через Account → 50000 ₽ ✅

#### 3. Кнопка Account:
**Добавлено:**
- Кнопка "Account" в главном меню
- Полноценная система управления профилями
- Возможность иметь несколько аккаунтов

### Тестирование:

```bash
# 1. Запустите бота
python3 farm_game_bot.py

# 2. Тесты для нового пользователя:
- Нажмите Account → Создать аккаунт → Проверьте баланс 50000 ₽
- Нажмите /myinfo → Должен показать баланс 50000 ₽

# 3. Тест продажи без животных:
- Нажмите /sell → Должно быть: "🚫 У вас нет животных для продажи"
- Кнопки выбора животных НЕ должны появиться
- Должен вернуться в главное меню

# 4. Тест создания второго аккаунта:
- Нажмите Account → Создать новый аккаунт → Введите другой ник
- Проверьте что баланс сбросился на 50000 ₽
- Старый аккаунт сохранен в БД
```

### Логи:

При работе с аккаунтами:
```
account_command: telegram_user=X, telegram_id=123456
create_account_confirm: user=X
process_nickname_input: telegram_user=X, nickname=Y
process_nickname_input: account created for Y
```

При продаже без животных:
```
show_available_goods: user X, setting desire='sell'
show_available_goods: no animals, cleared desire
```
