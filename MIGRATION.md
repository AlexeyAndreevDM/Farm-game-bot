# 🔄 Инструкция по миграции на новую версию

## Что изменилось?

Ваш бот был реструктурирован из одного файла в модульную архитектуру.

### ⚠️ ВАЖНО: Оригинальный файл сохранен!
Ваш старый код: `farm_game_bot.py.backup`

---

## 📝 Быстрый старт

### 1. Проверьте настройки

Откройте файл `.env` и убедитесь, что токен указан правильно:
```
BOT_TOKEN=ваш_токен_здесь
```

### 2. Установите зависимости (если не установлены)

```bash
pip install -r requirements.txt
```

или

```bash
pip3 install -r requirements.txt
```

### 3. Проверьте конфигурацию

```bash
python3 check_setup.py
```

Вы должны увидеть:
```
✅ Все проверки пройдены! Бот готов к запуску.
```

### 4. Запустите бота

```bash
python3 farm_game_bot.py
```

---

## 🗂️ Что где находится?

| Что нужно изменить | Где искать |
|-------------------|-----------|
| Токен бота | `.env` |
| Цены на животных | `config/settings.py` → `get_animal_prices()` |
| Тексты сообщений | `utils/messages.py` |
| Клавиатуры | `utils/keyboards.py` |
| Логика покупки/продажи | `handlers/shop.py` |
| Команда /start | `handlers/start.py` |
| Регистрация | `handlers/registration.py` |
| Информация и топ | `handlers/info.py` |
| Работа с БД | `database/db_manager.py` |

---

## 🔍 Примеры изменений

### Изменить текст приветствия

Откройте `utils/messages.py` и найдите:
```python
WELCOME_MESSAGE_NEW = """Привет, {}! ..."""
```

### Изменить цены

Откройте `config/settings.py` и найдите:
```python
def get_animal_prices():
    return {
        'Корову': randint(8500, 13000),  # ← Измените здесь
        ...
    }
```

### Добавить новую команду

1. Создайте функцию в нужном handler (например, `handlers/info.py`)
2. Зарегистрируйте её через `@bot.message_handler(...)`
3. Перезапустите бота

---

## 🐛 Если что-то не работает

### Проблема: "BOT_TOKEN not found"
**Решение:** Проверьте файл `.env`, токен должен быть указан

### Проблема: "Module not found"
**Решение:** Установите зависимости: `pip install -r requirements.txt`

### Проблема: База данных не работает
**Решение:** Убедитесь, что файл `farmers.db` существует или будет создан автоматически

### Проблема: Бот не отвечает на команды
**Решение:** Проверьте логи в файле `bot.log`

---

## 📊 Логи

Все действия бота логируются в файл `bot.log`. При возникновении проблем смотрите туда.

Пример:
```bash
tail -f bot.log  # Следить за логами в реальном времени
```

---

## 🔙 Как вернуться к старой версии?

Если новая версия не подходит:

```bash
cp farm_game_bot.py.backup farm_game_bot.py
```

И запустите как раньше.

---

## 📚 Дополнительная информация

- **Полное описание изменений:** `CHANGES.md`
- **Документация проекта:** `README.md`
- **Структура кода:** Все модули имеют docstrings

---

## ✅ Чек-лист перед запуском

- [ ] Файл `.env` создан и содержит токен
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] `check_setup.py` прошел успешно
- [ ] Резервная копия старого кода есть (`farm_game_bot.py.backup`)

---

## 🎉 Готово!

Теперь ваш бот имеет профессиональную структуру и готов к развитию!

Если есть вопросы - изучите документацию в модулях (каждая функция имеет описание).
