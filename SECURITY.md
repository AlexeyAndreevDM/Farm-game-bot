# 🔐 Инструкция по безопасности токена

## ✅ Что уже сделано:

1. **.env в .gitignore** - файл не попадет в Git
2. **Pre-commit hook** - блокирует коммит .env
3. **.env НЕ отслеживается** Git

## 📝 Как добавить новый токен:

1. Откройте файл `.env`
2. Найдите строку `BOT_TOKEN=...`
3. Замените на ваш новый токен от @BotFather:
   ```
   BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

## 🚀 Запуск бота:

```bash
python3 farm_game_bot.py
```

## 🛡️ Защита работает:

Если попытаетесь добавить .env в Git:
```bash
git add .env
git commit -m "test"
```

Вы получите ошибку:
```
❌ ОШИБКА: Попытка закоммитить .env файл с секретами!
```

## 🧹 Очистка старых коммитов (опционально):

Если хотите удалить .env из истории Git:

```bash
git filter-branch --force --index-filter \
  'git rm --cached --ignore-unmatch .env' \
  --prune-empty --tag-name-filter cat -- --all

git push origin --force --all
```

⚠️ **ВНИМАНИЕ:** Это перепишет историю! Используйте только если уверены.

## ✅ Теперь можно безопасно коммитить!

```bash
git add .
git commit -m "Your changes"
git push
```

Файл .env останется только локально и никогда не попадет на GitHub.
