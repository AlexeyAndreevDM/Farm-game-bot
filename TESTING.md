# 🧪 Инструкция по тестированию обновлений

## Что тестируем:
1. ✅ Автоматическое обновление цен раз в сутки
2. ✅ Систему рейтинга игроков (total_assets)
3. ✅ Пересчет активов при покупке/продаже
4. ✅ Миграцию базы данных

---

## 📝 Пошаговое тестирование

### Шаг 1: Запуск бота

```bash
cd "/Users/aleksej/Documents/тг_бот(ферма)/Farm-game-bot"
python3 farm_game_bot.py
```

**Ожидаемое в логах:**
```
INFO - Database initialized successfully
INFO - Added total_assets column to database  (если первый запуск)
INFO - Generated new prices. Next update: 19.12.2025 14:30
INFO - Bot initialized successfully
INFO - All handlers registered successfully
INFO - Bot is starting...
```

---

### Шаг 2: Проверка миграции БД

```bash
# В другом терминале проверить структуру БД:
sqlite3 farmers.db "PRAGMA table_info(goods);"
```

**Должно быть поле:**
```
5|total_assets|INTEGER|0||0
```

---

### Шаг 3: Тестирование в боте

#### 3.1. Регистрация (если еще не зарегистрированы)
```
Отправить: /start
Отправить: /reg
```

**Проверить:**
- [ ] Регистрация прошла успешно
- [ ] total_assets = 50000 (начальный капитал)

#### 3.2. Проверка топа
```
Отправить: /top
```

**Ожидаемый результат:**
```
Так, посмотрим...
Ваш место в топе фермеров: [число]

🏆 ТОП-10 ФЕРМЕРОВ:
🥇 Имя - сумма ₽
🥈 Имя - сумма ₽
...
```

**Проверить:**
- [ ] Отображается реальное место
- [ ] Показывается топ-10
- [ ] Есть медали 🥇🥈🥉

#### 3.3. Проверка инфо
```
Отправить: /myinfo
```

**Проверить:**
- [ ] Показывается реальное место в топе
- [ ] Отображается баланс
- [ ] Видны животные

#### 3.4. Покупка животных
```
Отправить: /buy
Выбрать: Курицу
Ввести: 5
```

**Проверить:**
- [ ] Покупка прошла успешно
- [ ] Баланс уменьшился
- [ ] После покупки: /myinfo - место может измениться

#### 3.5. Проверка пересчета total_assets

```bash
# В терминале проверить БД:
sqlite3 farmers.db "SELECT us_name, money, total_assets FROM goods;"
```

**Должно быть:**
```
us_name | money | total_assets
------- | ----- | ------------
Имя     | 45000 | ~47500  (деньги + стоимость 5 куриц)
```

**total_assets должен быть:** деньги + (цена_продажи_курицы × 5)

---

### Шаг 4: Проверка обновления цен

#### 4.1. Проверить текущее время обновления

```bash
cat prices_cache.json
```

**Должно быть:**
```json
{
  "animal_prices": {...},
  "sell_prices": {...},
  "last_update": "2025-12-18T14:30:00"
}
```

#### 4.2. Ручное тестирование обновления

Два способа:

**Способ 1: Изменить время в кэше**
```bash
# Отредактировать prices_cache.json
# Изменить last_update на вчерашний день
# Перезапустить бота
```

**Способ 2: Изменить интервал**
```python
# В config/settings.py временно изменить:
PRICE_UPDATE_INTERVAL_HOURS = 0.01  # 36 секунд
```

Перезапустить бота и подождать ~1 минуту.

**В логах должно быть:**
```
INFO - Auto-updating prices
INFO - Generated new prices
INFO - Updated total_assets for all players after price change
```

**Проверить в БД:**
```bash
sqlite3 farmers.db "SELECT us_name, total_assets FROM goods;"
```

**total_assets должен пересчитаться для ВСЕХ игроков!**

---

### Шаг 5: Стресс-тест рейтинга

#### 5.1. Создать несколько тестовых игроков
- Зарегистрировать 3-5 игроков
- Купить им разное количество животных
- Проверить топ: `/top`

**Проверить:**
- [ ] Игроки сортируются по total_assets
- [ ] Показываются правильные суммы
- [ ] Место каждого игрока корректно

#### 5.2. Изменить цены и проверить пересчет

```bash
# Удалить кэш цен
rm prices_cache.json
# Перезапустить бота
```

**В логах:**
```
INFO - Generated new prices
INFO - Updated total_assets for all players
```

**Отправить:** `/top`

**Проверить:**
- [ ] Порядок игроков мог измениться
- [ ] Суммы пересчитались с новыми ценами

---

## 🔍 Что проверять в логах

### При запуске:
```
INFO - Database initialized successfully
INFO - Generated new prices. Next update: ...
```

### При покупке:
```
INFO - User <name> updated successfully
```

### При обновлении цен:
```
INFO - Auto-updating prices
INFO - Generated new prices
INFO - Updated total_assets for all players after price change
```

### Ошибки (не должно быть):
```
ERROR - ...
```

---

## ✅ Чеклист финального теста

- [ ] Бот запускается без ошибок
- [ ] Миграция БД прошла (поле total_assets добавлено)
- [ ] Цены генерируются и сохраняются в кэш
- [ ] `/top` показывает реальный топ с медалями
- [ ] `/myinfo` показывает реальное место
- [ ] При покупке total_assets пересчитывается
- [ ] При изменении цен total_assets пересчитывается для всех
- [ ] Файл prices_cache.json создается
- [ ] Логи пишутся в bot.log

---

## 🐛 Возможные проблемы

### Проблема: "No such file or directory: 'prices_cache.json'"
**Решение:** Это нормально при первом запуске. Файл создастся автоматически.

### Проблема: "no such column: total_assets"
**Решение:** Миграция не прошла. Удалить БД и запустить заново:
```bash
rm farmers.db farmers.db-journal
python3 farm_game_bot.py
```

### Проблема: Цены не обновляются
**Решение:** Проверить `PRICE_UPDATE_INTERVAL_HOURS` в config/settings.py

### Проблема: Место в топе не меняется
**Решение:** Убедиться, что total_assets пересчитывается:
```bash
sqlite3 farmers.db "SELECT * FROM goods;"
```

---

## 📊 Ожидаемые результаты

После всех тестов:

1. ✅ БД имеет поле `total_assets`
2. ✅ Все игроки имеют корректный `total_assets`
3. ✅ Цены обновляются раз в сутки
4. ✅ При обновлении цен пересчитываются активы
5. ✅ Топ показывает реальных лидеров
6. ✅ Место в рейтинге корректное

---

## 🎯 Быстрый тест (2 минуты)

```bash
# 1. Запустить бота
python3 farm_game_bot.py

# В боте:
# 2. /start
# 3. /reg (если нужно)
# 4. /top - увидеть топ
# 5. /myinfo - увидеть место
# 6. /buy → Курицу → 5
# 7. /myinfo - место должно измениться

# 8. Проверить БД:
sqlite3 farmers.db "SELECT us_name, money, total_assets FROM goods;"

# 9. Проверить кэш цен:
ls -la prices_cache.json
cat prices_cache.json
```

**Если все работает - тест пройден! ✅**
