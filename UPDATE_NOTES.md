# Обновления бота - Система цен и рейтинга

## ✅ Реализованные изменения

### 1. 📊 Система обновления цен раз в сутки

#### Что было:
- Цены генерировались один раз при запуске бота
- Не обновлялись автоматически
- Нельзя было узнать время следующего обновления

#### Что стало:
- ✅ Создан `PriceManager` для управления ценами
- ✅ Цены автоматически обновляются каждые 24 часа
- ✅ Цены сохраняются в кэш-файл `prices_cache.json`
- ✅ При обновлении цен пересчитывается `total_assets` всех игроков

**Файлы:**
- `utils/price_manager.py` - новый модуль управления ценами
- `config/settings.py` - добавлена константа `PRICE_UPDATE_INTERVAL_HOURS = 24`

**Как это работает:**
```python
# Цены автоматически проверяются при каждом запросе
animal_prices = price_manager.get_animal_prices()
sell_prices = price_manager.get_sell_prices()

# Если прошло 24 часа - генерируются новые цены
# И обновляется total_assets для всех игроков
```

---

### 2. 🏆 Система рейтинга игроков (total_assets)

#### Что было:
- Топ не работал (заглушка возвращала 3 место)
- Нет поля для подсчета суммарной стоимости активов
- Невозможно сравнить игроков по реальным достижениям

#### Что стало:
- ✅ Добавлено поле `total_assets` в БД
- ✅ Автоматическая миграция существующих записей
- ✅ Пересчет `total_assets` при каждой покупке/продаже
- ✅ Пересчет для всех игроков при изменении цен
- ✅ Реальный топ-10 игроков с медалями 🥇🥈🥉
- ✅ Определение реального места игрока в рейтинге

**Формула подсчета:**
```
total_assets = деньги + стоимость_всех_животных
```

где стоимость животного = цена продажи × количество

---

## 📁 Измененные файлы

### Новые файлы:
- `utils/price_manager.py` - менеджер цен с автоматическим обновлением

### Обновленные файлы:

#### `config/settings.py`
- Добавлена константа `PRICE_UPDATE_INTERVAL_HOURS = 24`

#### `database/db_manager.py`
- Добавлено поле `total_assets` в таблицу `goods`
- Миграция для существующих записей
- Метод `get_user_rank()` - получение места в топе
- Метод `update_all_total_assets()` - пересчет для всех при изменении цен
- Обновлены методы `create_user()` и `update_user()`

#### `handlers/shop.py`
- Замена статических цен на `price_manager`
- Автоматический пересчет `total_assets` при покупке
- Функция `update_user_data()` теперь пересчитывает активы

#### `handlers/info.py`
- Команда `/myinfo` показывает реальное место в топе
- Команда `/top` показывает топ-10 игроков с медалями
- Отображение стоимости активов

#### `handlers/registration.py`
- При регистрации инициализируется `total_assets = INITIAL_MONEY`

#### `handlers/callbacks.py`
- Использование `price_manager` вместо статических цен

#### `.gitignore`
- Добавлен `prices_cache.json` в игнор

---

## 🚀 Как это работает

### Обновление цен:

1. **При первом запуске:**
   - Генерируются случайные цены
   - Сохраняются в `prices_cache.json` с отметкой времени

2. **При последующих запусках:**
   - Загружаются цены из кэша
   - Проверяется время последнего обновления
   - Если прошло >= 24 часов → генерируются новые цены

3. **При генерации новых цен:**
   - Обновляются все цены
   - Пересчитывается `total_assets` для ВСЕХ игроков
   - Сохраняются в кэш

### Подсчет рейтинга:

1. **При покупке/продаже:**
   ```python
   # Подсчет стоимости животных
   animals_value = сумма(цена_продажи × количество)
   total_assets = money + animals_value
   # Сохранение в БД
   ```

2. **При изменении цен:**
   ```python
   # Для каждого игрока:
   animals_value = пересчет_с_новыми_ценами()
   total_assets = money + animals_value
   ```

3. **Определение места:**
   ```sql
   -- Считаем сколько игроков имеют больше активов
   SELECT COUNT(*) + 1 FROM goods 
   WHERE total_assets > user_total_assets
   ```

---

## 📊 Примеры использования

### Получение цен:
```python
from utils.price_manager import price_manager

# Автоматически проверяет нужно ли обновить
animal_prices = price_manager.get_animal_prices()
sell_prices = price_manager.get_sell_prices()

# Узнать время до следующего обновления
time_left = price_manager.get_time_until_update()
# Вернет: "15ч 30мин" или "скоро"

# Принудительное обновление (для админа)
price_manager.force_update()
```

### Работа с рейтингом:
```python
from database.db_manager import db_manager

# Получить место игрока
rank = db_manager.get_user_rank(us_name)  # 1, 2, 3...

# Получить топ-10
top_users = db_manager.get_top_users(10)
# [(name, total_assets), ...]

# Обновить для всех при изменении цен
db_manager.update_all_total_assets(animal_prices, sell_prices)
```

---

## 🎮 Что видит игрок

### Команда /myinfo:
```
Твоя информация:
Никнейм: Алексей
Твой бюджет: 75000
Твое место в топе: 5.    ← РЕАЛЬНОЕ МЕСТО
Коров: 2
Свиней: 5
...
```

### Команда /top:
```
Так, посмотрим...
Ваш место в топе фермеров: 5

🏆 ТОП-10 ФЕРМЕРОВ:

🥇 Иван - 250,000 ₽
🥈 Мария - 180,000 ₽
🥉 Петр - 150,000 ₽
4. Алексей - 120,000 ₽
5. Ольга - 95,000 ₽
...
```

---

## 🔧 Настройка

### Изменить частоту обновления цен:

В файле `config/settings.py`:
```python
# Частота обновления цен (в часах)
PRICE_UPDATE_INTERVAL_HOURS = 24  # Изменить на нужное значение
```

### Сбросить кэш цен:

Удалить файл `prices_cache.json` - при следующем запуске цены будут пересгенерированы.

---

## 🐛 Миграция БД

Миграция происходит автоматически при первом запуске:

1. Проверяется наличие поля `total_assets`
2. Если его нет - добавляется со значением по умолчанию 0
3. При первом действии игрока - пересчитывается

**Для существующих игроков:**
- При следующем запросе цен их `total_assets` будет пересчитан
- Или можно принудительно: `price_manager.force_update()`

---

## ✅ Тесты

Для проверки работы:

```bash
# Запустить бота
python3 farm_game_bot.py

# В боте:
# 1. /myinfo - проверить место в топе
# 2. /top - увидеть топ-10
# 3. /buy - купить животных
# 4. /myinfo - место должно измениться
```

---

## 📝 Логи

Все действия логируются:

```
INFO - Generated new prices. Next update: 19.12.2025 14:30
INFO - Updated total_assets for all players after price change
INFO - User aleksej updated successfully
INFO - Auto-updating prices (прошло 24 часа)
```

Смотреть логи: `tail -f bot.log`

---

## 🎯 Итого

✅ Цены обновляются автоматически раз в 24 часа
✅ При обновлении цен пересчитываются активы всех игроков  
✅ Топ игроков работает на основе реальных данных
✅ Место в рейтинге отображается корректно
✅ Миграция БД происходит автоматически
✅ Все изменения логируются

**Логика игры осталась прежней, добавлена только автоматизация!**
